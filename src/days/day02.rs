use aoc2022_lib::{utils, Day, Part};

#[derive(Default)]
pub struct Day02 {}

impl<'a> Day<'a> for Day02 {
    fn solve(&mut self, input: &'a str, part: Part) -> String {
        if part.is_first() {
            return String::new();
        }
        String::new()
    }
}
