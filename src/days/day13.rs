use aoc2022_lib::{utils, Day, Part};
use text_io::scan;

#[derive(Default)]
pub struct Day13 {}

impl<'a> Day<'a> for Day13 {
    fn solve(&mut self, input: &'a str, part: Part) -> String {
        if part.is_first() {
            return String::new();
        }
        String::new()
    }
}
